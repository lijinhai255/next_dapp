version: 1
frontend:
  phases:
    preBuild:
      commands:
        - export NODE_OPTIONS="--max-old-space-size=2048" # 或更高值
        - yarn install
    build:
      commands:
        - export NODE_OPTIONS="--max-old-space-size=2048" # 或更高值
        - yarn build